## CI Automation

This project includes a GitHub Actions workflow that automatically runs
the simulated build and CI step on every push or pull request. This
mirrors Jenkins-style CI pipelines and reinforces foundational CI/CD
concepts.
